# 2 - Matrices #

### For E matrix ###

# Parameters
mm.file <- NULL
weight.file <- NULL                  # Path to weight file, if NULL no weights are used
ctr <- TRUE                          # Valid only if markers is NOT NULL
std <- TRUE                          # Valid only if markers is NOT NULL
weighting <- !is.null(weight.full)   # Valid only if markers is NOT NULL

prop.maf.j <- NULL                   # Proportion of minor allele freq of SNPs/markers

# Outputs
# EVD.rda
# G.rda
# Eigen.pdf

